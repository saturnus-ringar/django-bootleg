- det går inte att toggla "Advanced search", den försvinner direkt

- sökikonen ligger fel:

- filtrering funkar inte på foreign key

- någon bugg i highlighten:
    http://localhost:8000/list/customer/?q=test&dql=billing_address.city.name%20~%20%22stock%22
    "test§"
    ... nä, det var inlagt så

- krasch: http://localhost:8000/list/customer/?payment_terms=1

- toggling av avancerad sök

- fixa höjden på avancerad sök

- länka till syntax för avancerad sök

- större text i avancerad sök

- annan färg på highligh i avancerad sök
        introspections = DjangoQLSchemaSerializer().serialize(
            DjangoQLSchema(query.model),

element.style {
}
.djangoql-completion .active
        )
djangoql-completion

- http://localhost:8000/product-cards/?coated_side_material=1
    ... "Inside" highlightas inte

http://localhost:8000/product-cards/?pre_corona_settings=on&setting_time=30
    ... "run pre corona?" ska vara boolean
    ... varför visas "Additional info"-kolumnen?

- http://localhost:8000/product-cards/?additional_info=kebab&setting_time=30
    ... krasch

- om det är med bland standardfälten, lägg inte till via request:
    http://localhost:8000/list/customer/?reference=2

- highlighta sök för alla parametrar:
    http://localhost:8000/product-cards/?additional_info=kebab&setting_time=30
    ... fixa så det highlightar i rätt kolumn bara (måste sätta attrs i tables då...)

- blir dålig highlight:
    "https://nbc.theflow.se/product-cards/?pre_corona_settings=on&setting_time=30"
        "on" highlightas
        ... highlighta inte <thead> tilla att börja med

- validera alla såna här vid uppstart
        visible_fields = ["name", "vat_number", "label_text", "payment_terms", "reference"]
        create_url = reverse_lazy("customer_create")
        search_fields = ["name", "contact_name", "email_address", "vat_number", "reference__username",
                         "reference_firstname", "reference_lastname", "product_cards__name"]
        filter_fields = ["payment_terms", "reference", "product_cards"]

- tror inte produkkortfiltreringen fungerar:
    https://nbc.theflow.se/list/customer/?product_cards=5
    ...jo, funkar

- kontrollera sökfält, att det inte är en ForeignKey, går ju inte att söka på ändå

Exception Value:
'date_created' cannot be specified for DismissedEvent model form as it is a non-editable field

- filtreringen funkar inte?

- filtreringen på "okänt" på funkar inte

- alias för django-log-tail

- lägg till BRANDING_LOGO i base_site.html
    + i dokumentationen

- lägg till FAVICON_FILE i base.html

add_standard_column
    ... får inte in typerna av kolumner... inte bra

- vad har textarean för widget egentligen?

- ajax-ladda "view" i listvyn
    ... lite svårt att få in elementen i django tables 2

- man borde visa fältet man söker på:
    http://localhost:8000/orders/?order_volume=60000

- checkboxen checkas inte i i filterformuläret
    ... det är för att det är en autocomplete

- autocomplete på alla text, integer osv. inputtar

- behöver sökknapp i filterformuläret

- inga resultat:
    http://localhost:8000/list/customer/?q=mikael.ollas%40nordictextile.se

- flera resultat:
    http://localhost:8000/list/customer/?q=Nordic%20Textile%20Converting%20AB

- generellt, fånga errors i ajax-requests och visa att det gått fel

- "URL" visas på NBC fast dom inte har några absoluta URLar
    ... för att BaseModel har:
        get_absolute_url_link

- blir fel i URLen då man bara söker:
    http://localhost:8000/list/customer/?&q=ex

- måste få till blank på första inputten i OrderStatus

- Product Cards ska också ha en blank som första i selecten:
    https://nbc.theflow.se/list/customer/

- konstig formatering på datumen:
    http://localhost:8000/orders/?status=0&q=2

- varför hittar den massa?
    http://localhost:8000/orders/?status=0&q=shish

- generell filtrering på modeller via specade fält som ligger till höger

- göra alla fält icke-required i filter-form

- få med q-parametern i filtreringen

- resetta-filter-knapp

- fylla i värden i filterformuläret i request

- krasch:
    http://localhost:8000/json/event/?q=n

- radera modell ska redirecta till där man var

- ordrar:
    /Users/stefan/Dev/envs/nbcab/lib/python3.7/site-packages/django/views/generic/list.py:88: UnorderedObjectListWarning: Pagination may yield inconsistent results with an unordered object_list: <class 'core.models.order.Order'> QuerySet.
  allow_empty_first_page=allow_empty_first_page, **kwargs)

- hantera det här:
    bootleg.system.shell.CommandException: The process [['sar', '-rh']] raised an error: Cannot open /var/log/sysstat/sa04: No such file or directory
    Please check if data collecting is enabled
    ... finns inte för den fjärde än verkar det som

- tillbaka-knapp i detail-view

- bredare sök i bootstrap 3

- loader på sökformuläret

- skriva ut hur många object som finns i listvyn

- overrida django tables boolean-rendering

- kräva inloggning på listvyer osv.
    http://localhost:8000/list/dismissedevent/

- dubbla alerts då man ska radera ett objekt

- sökformuläret syns inte för dismissed-event

- autocompleta inte på URL-fält och textfält

- funkar inte:
    http://localhost:8000/view/dismissedevent/203741

- default-sortering = id, men att kunna välja sortering

- kunna disabla skapande och radering

- sidor för att hantera loggade exceptions och django log exceptions

a konfig för PROJECT_ABBR , istället för prefixgrejen

- fixa ett alias för status nbcstatus -> pm status

- snyggare paginering

- setup verkar inte fixa filrättigheterna rekursivt

- setup-loggern skriver inte till standard out

- bootleg-error-loggarna med felkod funkar inte

- göra en test-sida för error-sidorna:
    /obscurity-by-test/error/<status_code>/
    ... kräv is_staff

- en krasch-url
    ... kräv is_staff

- en ping-url
    ... man kan använda dev-null

- skriva ut i system att det är fejkdata om man använder textfilerna

- bootleg-loggers ska heta bootleg.forms osv.

- göra så man hämtar loggers med namn och filnamn:
    get_logger(filename=None, name=None)
    ... django slås t.ex. inte upp på filename

- göra default-error-sidor så man inte måste ha dom i settings, men varna då dom används

- statuskommando, lista:
    diskutrymme
    databasstorlek och kanske antal rader i databasen

        *** TODO ***
        log-dir-storlek
        nuvarande commit

- bokstavssortering i
    get_subclasses
    ... den verkar bara funka om dom är i samma fil dessutom

- inte hårdkodad URL för javascripten i bootleg.js

- få över django-settings date format till bootleg.js

- göra sökformuläret mindre

- fokusera på den första inputten som inte har värde...
    t.ex. lösenord då login failar

- ta bort listpunkten på error då login failar

template = "debug_toolbar/panels/settings.html"

nav_title = _("Settings")

def title(self):
    return _("Settings from %s") % settings.SETTINGS_MODULE

def generate_stats(self, request, response):
    self.record_stats(
        {
            "settings": OrderedDict(
                sorted(get_safe_settings().items(), key=lambda s: s[0])
            )
        }
    )


- fixa sticky-menyn i system


- fixa så man fångar exception i manage-kommandon också
    ... går inte att göra tror jag - man måste ändra i manage.py

- en konfigparameter för om man ska använda profile

- länk för att uppdatera:
    http://localhost:8000/list/kebab/

- update verkar inte funka:
    http://localhost:8000/update/kebab/12

- exception om get_logger får filnamn med extension, typ shish/shish.log

- rendera login i menyn

- could not be reversed
    ... index
    ... har den i instagram urls

- autofokus, funkar inte här iaf.
    http://localhost:8000/create/process/

- kunna sätta en Custom-profil-grunka som extendar Profile

- Last Modified är tomt här:
    http://localhost:8000/system/info/

- ingen favicon syns
    ... i django_tango va?

- att sätta
    LOGIN_REDIRECT_URL
    ... funkar inte med nbcab , AppRegistryNotReady

    får cirkulär import om jag kör det här:
            url = get_setting("LOGIN_REDIRECT_URL")
            #if url == global_settings.LOGIN_REDIRECT_URL:
                # override django's default url
            #    url = str(reverse_lazy("bootleg:dev_null"))

# TODO: fix dynamic URL
LOGIN_REDIRECT_URL = "index"

CSS_FILES = []
... validera att det är en lista
... och att filerna finns

MAIN_USER	/Users/stefan/Dev/projects/nbcab
MAIN_USER_GROUP	/Users/stefan/Dev/projects/nbcab
PROJECT_NAME	/Users/stefan/Dev/projects/nbcab
ROOT_FOLDER	/Users/stefan/Dev/projects/nbcab

- om det kraschar i system.py på t.ex.:

    def get_env(self):
        print(asdf)

    ... så hamnar inte det i django.log live, men gör det lokalt om DEBUG är False
    ... men det hamnar i DjangoLogEntry för båda

- sökning i javascriptfel och exceptions

- lösenordet skrivs ut i URLen här:
    http://localhost:8000/deploy/info/

- dubbla slash i github-urlen
    http://localhost:8000/deploy/info/

- humaniza datumet (också?) här:
    http://localhost:8000/deploy/info/
    ... ta bort sekunder också - äh det tar från datuminställningar

- krasch:
    http://localhost:8000/django-admin/bootleg/javascripterror/5/change/?_changelist_filters=handled__exact%3D0

- javascriptvarningar i menyn

- länk för att uppdatera:
    http://localhost:8000/list/kebab/

- update verkar inte funka:
    http://localhost:8000/update/kebab/12

- inkludera NAVIGATION_TEMPLATE

- fixa print_at_startup

- göra så man måste ange LOG_DIR
    ... skit i /dev/null-grejer
    ... kolla att det funkar i django_tango

- måste krascha här om det inte funkar:
        if file_system.is_writable(bootleg_settings.LOG_DIR):

- bootleg_settings.ADMIN_TEMPLATE
    ... verkar inte funka


- fixa så det här hanteras bra i settings.py:
    log_dir = getattr(settings, "LOG_DIR")
    django_log_level = getattr(settings, "DJANGO_LOG_LEVEL", "INFO")

- det spars inga DjangoLogEntries

- kolla att alla error-templates finns

- göra så man kan använda settings som django, a la:
    bootleg_settings.TEMPLATE_404

- tror inte att error-sidorna varken inkluderas eller funkar

- error-sidor osv. funkar nog inte riktigt
    ... alla 500 hamnar nog inte i django-loggen


- man ska inte använda:
    if not settings.is_overridden("SITE_ID"):
    ... bara getattr(setttings

problem med settings:

django_tango importerar från bootleg:
    from bootleg.utils.utils import is_testing

    då kommer inte django_tangos settings efter det läsas in. Flödet:

    django: _setup
    django_tango: settings.py
    django_tango: settings_apps.py
    django: _setup
    bootleg: Settings.__init__
    django_tango: settings_custom.py
    django_tango: settings_local.py
    bootleg: apps.py setup()

- funkar icke:
    http://localhost:8000/create/kebab/

- funkar icke:
    http://localhost:8000/list/kebab/

- sätta APPEND_SLASH?!

- kontroller av ägare/rättigheter på:

- tag för att rendera deploy-status osv.

- namespaca URLar (i alla projekt)

- visa deploy och system i menyn

from django.utils.version import get_version

VERSION = (3, 1, 0, 'final', 0)

__version__ = get_version(VERSION)


- funkar t.ex. SITE_ID inställningen?

med debug false får man bara det här i loggen:

    2020-10-30 16:12:29 ERROR Internal Server Error: /ical/199476/event.ical

- lägg in django-debug-toolbar

- när importeras settings.py egentligen?
    ... det är setup.py som gör det

kör det här i __init__ setup, typ

    # read settings to validate at startup
    bootleg_settings.navigation_template()

    if bootleg_settings.add_builtins():
        logging.add_builtins()

gör bootstrap till en funktion

***************************************
 lösenordsreset/login/byta lösenord
***************************************

- kunna lägga till URLar till EXEMPT_URLS i LoginMiddleWare

- fixa en default-index-sida?

- next på login

- ha en POST_LOGIN_URL i konfen
    ... som inte tar med next isf.

- lägga in lösenordsresetgrejen

- fixa dango meddelanden
    ... tag?
    ... det är ju vajsing med klasser på dom som inte matchar bootstraps

- skapa default-site från settings

- reverse-url till django-admin

- setting för USE_GENERIC_MODELS, default True
    ... behövs inte

- en tag för bootleg-delen av menyn
    ... loggade saker
    ... dropdown med profil + django-admin etc.
    ... editable models

- "DJANGO LOG ENTRIES" har ingen siffra

- funkar loggningen med hanterade loggade saker?
    ... ja

***************************************
 templates
***************************************

    - göra någon URL för favicon.ico

    - konfig för bootstrap-css

    - ta två första bokstäverna från LANGUAGE_CODE

    - man bör kunna använda: BASE_TEMPLATE

    - messages funkar icke tror jag

    - göra en bas-template, som man kan skita i helt att ha med:
        en boostrap-bas, med:
            - djangos-meddelanden
            - valbar content-container-klass
            - google-analytics, om konto är angett
            - default-css som kan overridas

            - block för:
                extra_css
                extra_js
                title
                meta_description

- få in javascriptfelgrejerna

- race conditions i logging.py då det skapas loggers

***************************************
 formulär
***************************************

    - tag för att rendera formulär

    - auto-fokus

    - loader


- får inte med menyn här:
    http://localhost:8000/list/kebab/

- fixa in django admin på /django/admin

- ska ha level på django-loggningen:
    'level': 'DEBUG',

- fixa:
    def get_formatter():
        return logging.Formatter(settings.LOGGING["formatters"]["verbose"]["format"],
                                 settings.LOGGING["formatters"]["verbose"]["datefmt"])

- logga alla failade formulär

- verkar inte som 500-templaten funkar

- varna om DJANGO_LOG_LEVEL är under INFO

använd settings.base_template() osv. osv.

